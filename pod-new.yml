---
- name: Create a Pod in Kubernetes
  hosts: all
  gather_facts: no
  collections:
    - community.kubernetes

  tasks:
    - name: krishna
      k8s:
        state: present
        definition:
          apiVersion: v1
          kind: Pod
          metadata:
            name: pod-01
            namespace: default
          spec:
            containers:
              - name: nginx
                image: nginx:latest
                ports:
                  - containerPort: 80
